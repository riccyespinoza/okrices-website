export const metadata = {
  title: "Project Detail",
  description: "Detailed view of a project",
};

export default function ProjectDetailLayout({ children }) {
  return <div className="project-detail-layout">{children}</div>;
}
